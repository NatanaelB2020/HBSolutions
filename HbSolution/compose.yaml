version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: hbsolution-mysql
    environment:
      MYSQL_DATABASE: HBsolution
      MYSQL_USER: hbsolutionadmin
      MYSQL_PASSWORD: Berlolo@0106
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    command: --default-authentication-plugin=caching_sha2_password
    volumes:
      - mysql_data:/var/lib/mysql
    restart: always
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "hbsolutionadmin", "-pBerlolo@0106"]
      interval: 15s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hbsolution-backend
    ports:
      - "8080:8080"
      - "5005:5005" # porta do debug remoto
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/HBsolution?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      SPRING_DATASOURCE_USERNAME: hbsolutionadmin
      SPRING_DATASOURCE_PASSWORD: Berlolo@0106
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_JPA_SHOW_SQL: "true"
      JWT_SECRET: "S3nh@UltraS3gur@!2025_AplicacaoHbSolution"
      JWT_EXPIRATION: 86400000
    depends_on:
      mysql:
        condition: service_healthy
    restart: always

volumes:
  mysql_data:
 